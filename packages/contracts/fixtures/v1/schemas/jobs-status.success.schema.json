{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nova/contracts/fixtures/v1/schemas/jobs-status.success.schema.json",
  "title": "JobsStatusSuccessResponse",
  "type": "object",
  "additionalProperties": false,
  "required": ["job"],
  "properties": {
    "job": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "job_id",
        "job_type",
        "scope_id",
        "status",
        "payload",
        "result",
        "error",
        "created_at",
        "updated_at"
      ],
      "properties": {
        "job_id": { "type": "string" },
        "job_type": { "type": "string" },
        "scope_id": { "type": "string" },
        "status": {
          "type": "string",
          "enum": ["pending", "running", "succeeded", "failed", "canceled"]
        },
        "payload": { "type": "object" },
        "result": { "type": ["object", "null"] },
        "error": { "type": ["string", "null"] },
        "created_at": { "type": "string", "format": "date-time" },
        "updated_at": { "type": "string", "format": "date-time" }
      }
    }
  }
}
