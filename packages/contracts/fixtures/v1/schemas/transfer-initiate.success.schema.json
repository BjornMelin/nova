{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nova/contracts/fixtures/v1/schemas/transfer-initiate.success.schema.json",
  "title": "TransferInitiateUploadSuccessResponse",
  "type": "object",
  "additionalProperties": false,
  "required": ["strategy", "bucket", "key", "session_id", "expires_in_seconds", "policy_id", "policy_version", "max_concurrency_hint", "sign_batch_size_hint", "accelerate_enabled", "checksum_mode", "resumable_until"],
  "properties": {
    "strategy": { "type": "string", "enum": ["single", "multipart"] },
    "bucket": { "type": "string" },
    "key": { "type": "string" },
    "session_id": { "type": "string" },
    "expires_in_seconds": { "type": "integer" },
    "policy_id": { "type": "string" },
    "policy_version": { "type": "string" },
    "max_concurrency_hint": { "type": "integer" },
    "sign_batch_size_hint": { "type": "integer" },
    "accelerate_enabled": { "type": "boolean" },
    "checksum_algorithm": { "type": ["string", "null"] },
    "checksum_mode": { "type": "string", "enum": ["none", "optional", "required"] },
    "resumable_until": { "type": "string", "format": "date-time" },
    "url": { "type": ["string", "null"] },
    "upload_id": { "type": ["string", "null"] },
    "part_size_bytes": { "type": ["integer", "null"] }
  }
}
