name: React-Next Minimal Nova Deploy Validate

on:
  workflow_dispatch:
    inputs:
      nova_deploy_output_json:
        required: true
        type: string

jobs:
  validate:
    uses: REPLACE_WITH_NOVA_REPO/.github/workflows/reusable-post-deploy-validate.yml@<pin-to-an-immutable-revision>
    with:
      deploy_repo: REPLACE_WITH_NOVA_REPO
      deploy_output_json: ${{ inputs.nova_deploy_output_json }}
